# LIONX — مصدر الحقيقة الموحد

## القرار التنفيذي

النسخة المعتمدة الحالية هي مشروع **LIONX Mobile** في المسار `/home/ubuntu/lionx-mobile`. هذا المشروع هو نقطة الانطلاق الوحيدة لمواصلة البناء. يعمل كقاعدة Expo/React Native لتطبيق الجوال، وتُستخدم واجهة Expo Web منه كنسخة الويب المتجاوبة المشتركة بدل إنشاء نسخة ثانية منفصلة في هذه المرحلة.

أي مشروع أو ملف ظهر في محادثة أخرى لا يُدمج تلقائيًا. يُستخدم فقط كمادة مرجعية بعد مقارنة محتواه بهذه الوثيقة، ولا يجوز أن يغيّر المعمارية أو يعيد فتح قرارات تم اعتمادها.

## ما نحتفظ به

نحتفظ بهوية LIONX FINAL MALL، وواجهة RTL، ونظام Black & Gold، والكتالوج الرقمي، والبحث والتصنيفات، وتفاصيل الخدمة، ومراجعة الطلب التجريبية، والمحفظة التجريبية، وشاشة الطلبات، والحساب، والمحتوى المشترك بين الجوال والويب.

نحتفظ أيضًا ببيانات الكتالوج المشتركة في `shared/catalog.ts`، وبنموذج الشاشات الحالية في `app/`، وبملف `design.md` و`todo.md` كسجلي تصميم وتنفيذ.

## ما نؤجله

يُؤجل الدفع الحقيقي، وتحويل الأموال، والمصادقة الإنتاجية، والمزامنة السحابية، وميزات VIP والمدونة الكاملة إلى أن يحدد المستخدم مزود الدفع، ونموذج الحساب، وقواعد التشغيل. النسخة الحالية تعرض تدفقات تجريبية ولا تتعامل مع أموال حقيقية.

## كيف نتعامل مع المحادثات القديمة

تُعامل محادثات LIONX السابقة، ونسخ Final Mall، وModule 2، وImport Protocol، وأي فروع متكررة على أنها **مراجع**. نأخذ منها فقط قرارًا أو متطلبًا واضحًا، ثم نسجله هنا وفي `todo.md`. لا ننسخ ملفات كاملة أو نخلط هياكل مشاريع متعددة.

## كيف نتعامل مع المهارات المذكورة

`content-gap-analysis` تُستخدم فقط عند طلب تحليل فجوات المحتوى أو SEO لموقع LIONX، وليست جزءًا من بناء المنتج الأساسي.

`manus-api` تُستخدم فقط إذا طلب المستخدم ربط LIONX بواجهة Manus API لإدارة مهام أو مشاريع أو نشر، وليست بديلًا عن Gemini API ولا مطلوبة لبناء الكتالوج الحالي.

`skill-creator` تُستخدم فقط عند طلب إنشاء مهارة جديدة أو تعديل مهارة موجودة، ولا تُستخدم لبناء صفحات التطبيق أو الموقع.

## قواعد العمل من الآن

لا ننشئ مشروع LIONX جديدًا ولا نسخة ثانية إلا بطلب صريح. قبل كل ميزة جديدة نضيف بندًا في `todo.md`، نحدد الملفات المطلوبة، ونفحص أثرها على الجوال والويب. قبل التسليم نراجع `todo.md`، نشغل TypeScript وlint والاختبارات، ونحفظ checkpoint واحدًا واضحًا للنسخة المعتمدة.

## الحالة الحالية

النسخة الحالية تحتوي على واجهة Home، والمتجر، وتفاصيل الخدمة، والمحفظة، ومراجعة الدفع التجريبية، والطلبات، والحساب، وتعمل على الجوال والويب من قاعدة كود واحدة. العوائق الرئيسية المتبقية هي الأصول النهائية للشعار، واختبارات منطقية مخصصة، وميزات VIP والمدونة، وربط الخدمات الحقيقية والدفع الحقيقي عند تحديد متطلباتها.
