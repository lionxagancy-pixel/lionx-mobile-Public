# دليل تشغيل وتنفيذ LIONX Web + Mobile

## 1. الحالة الحالية

مشروع LIONX الموحد موجود في `lionx-mobile` ويستخدم Expo/React Native مع Expo Router. نفس قاعدة الكود تنتج تطبيقًا للجوال ونسخة Web متجاوبة باللغة العربية وRTL وهوية Black & Gold. آخر تعديل تشغيلي هو ربط زر تأكيد الطلب برقم WhatsApp الرسمي بصيغة دولية `201279332563`.

أثبت البناء الأخير أن Expo Web يُصدّر 23 مسارًا، منها الرئيسية، المتجر، المحفظة، الطلبات، الحساب، Checkout، المساعد، تتبع الطلب، القطاعات، تفاصيل الخدمة، نجاح الطلب، Quote Builder، المحتوى الديناميكي وطرق الدفع.

> **حدود مهمة:** الدفع الحالي يدوي للمراجعة، ورفع الإيصال المحلي لا يمثل تخزينًا خادميًا إنتاجيًا. إصدار بطاقات LionxPay ليس مفعّلًا، وتحديث أسعار الموردين ورفع الصور الآلي يحتاجان تكاملات خارجية معتمدة.

## 2. رابط المعاينة

الرابط المرجعي المنشور الذي يعمل للمعاينة هو: https://lionxmall-hq6fkenr.manus.space/

أما نسخة المشروع الموحد الأخيرة فتحتاج نشرها من لوحة المشروع بعد إنشاء checkpoint جديد. لا تستخدم أي رابط ينتهي بنقطة، وافتح الرابط في نافذة خاصة إذا ظهر إصدار قديم بسبب cache.

## 3. ما يراه العميل

يبدأ العميل من Hero المول، ثم يختار أفرادًا أو شركات، يبحث باسم الخدمة أو الكود، ويستعرض PLAY وPAY وGROW وDIGITAL وVIP. يستطيع فتح تفاصيل الخدمة وإضافتها إلى السلة، ثم اختيار طريقة الدفع اليدوي وإرفاق اسم صورة أو PDF للإيصال، وإنشاء طلب برقم مرجعي.

بعد إنشاء الطلب تظهر شاشة نجاح الطلب بحالة مراجعة يدوية وزر يفتح WhatsApp برسالة جاهزة تحتوي رقم الطلب والإجمالي والحالة واسم الإيصال. رقم التواصل الرسمي هو 01279332563، ويُستخدم داخليًا بصيغة `201279332563`. لا تعتبر الرسالة أو الإيصال وحدهما إثباتًا لنجاح الدفع حتى يراجعه فريق LIONX.

## 4. تشغيل نسخة Web محليًا

```bash
cd /home/ubuntu/lionx-mobile
pnpm install
pnpm dev
```

لبناء نسخة إنتاج Web وخادمها:

```bash
pnpm build
NODE_ENV=production node dist/index.js
```

اختبارات الجودة:

```bash
pnpm check
pnpm lint
pnpm test
pnpm build
```

## 5. تشغيل تطبيق Expo

للمعاينة عبر Web استخدم `pnpm dev:metro`. وللمعاينة على الهاتف، افتح مشروع Expo من لوحة المشروع أو استخدم QR الذي توفره بيئة Expo. لا تضع أسرار مزودي الدفع أو البطاقات داخل التطبيق أو `app.config.ts`.

## 6. ما يلزم للتشغيل التجاري

يلزم اعتماد رقم WhatsApp وسياسة استقبال الطلبات، ثم تخزين آمن للإيصالات، لوحة Admin وRBAC، كتالوج مورد موثق، محرك تسعير يحفظ تكلفة المورد وهامش LIONX ونسخة السعر، ومزود دفع معتمد. بعد ذلك يمكن إضافة WhatsApp Cloud API من الخادم بدل فتح رابط WhatsApp، مع Webhooks وسجل تدقيق.

## 7. التسلسل التنفيذي المقترح

ابدأ بمورد واحد وقطاع واحد، واستورد 20–50 خدمة فقط، ثم اختبر الأسعار والصور وتحديث المخزون والطلب اليدوي. بعد ثبات دورة الطلب، أضف مزود دفع واحدًا في Sandbox، ثم لوحة الإدارة، ثم التوسع إلى باقي الموردين. يعزل هذا الترتيب المخاطر ويمنع بناء تكاملات كثيرة قبل إثبات نموذج العمل.

## 8. الملفات المرجعية المهمة

`LIONX_SOURCE_OF_TRUTH.md` هو المرجع العام. `LIONX_COMPLETION_GAPS.md` يوضح فجوات الإطلاق. `LIONXPAY_CARD_ISSUING_REQUIREMENTS.md` يوضح متطلبات إصدار البطاقات. `LIONXPAY_BROTHER_ONBOARDING_PACKAGE.md` جاهز لإرساله إلى الشريك الذي سيتواصل مع مزودي الإصدار. `LIONX_FINAL_MALL_COPY_FULL_AUDIT.md` يوضح الفرق بين المرجع القديم والنسخة الموحدة.
