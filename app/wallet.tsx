import { Pressable, ScrollView, Text, View } from "react-native";
import { useRouter } from "expo-router";
import { ScreenContainer } from "@/components/screen-container";

export default function WalletScreen() {
  const router = useRouter();
  return <ScreenContainer edges={["top", "bottom", "left", "right"]} className="px-4 pt-3"><ScrollView contentContainerStyle={{ paddingBottom: 30 }}><Pressable onPress={() => router.back()}><Text className="text-sm font-bold text-primary">← العودة</Text></Pressable><Text className="mt-6 text-3xl font-black text-foreground">المحفظة</Text><Text className="mt-2 text-sm text-muted">إدارة رصيدك وعملياتك في مكان واحد.</Text><View className="mt-6 rounded-3xl bg-primary p-6"><Text className="text-xs font-bold text-background">الرصيد المتاح · تجريبي</Text><Text className="mt-3 text-4xl font-black text-background">0.00 ج.م</Text><Pressable onPress={() => router.push("/checkout")} style={({ pressed }) => [{ opacity: pressed ? 0.8 : 1, marginTop: 20 }]}><View className="rounded-2xl bg-background px-5 py-3"><Text className="text-center font-black text-primary">إضافة رصيد</Text></View></Pressable></View><View className="mt-5 rounded-2xl border border-border bg-surface p-5"><Text className="font-black text-foreground">آخر العمليات</Text><Text className="mt-4 text-sm text-muted">لا توجد عمليات مسجلة بعد.</Text></View><View className="mt-4 rounded-2xl border border-warning/40 bg-surface p-5"><Text className="font-bold text-warning">تنبيه</Text><Text className="mt-2 text-sm leading-5 text-muted">هذه نسخة أولية؛ لن يتم خصم أو تحويل أموال حقيقية.</Text></View></ScrollView></ScreenContainer>;
}
